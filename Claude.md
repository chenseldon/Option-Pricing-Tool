# 期权定价工具\&amp;交易报表脚本 Claude指令文档

# 项目1：Python全品类期权定价工具 Claude\.md

\#\#\# 指令说明：请帮我编写一个Python全品类期权定价工具，用于场外衍生品交易助理岗位的GitHub个人项目，贴合校招场景，代码简洁规范、可直接运行，附带注释和运行说明，适配券商场外期权定价核心需求。

\#\#\# 核心需求（必实现）

1\.  技术栈：使用py\_vollib（核心定价库）、numpy（数值计算）、pandas（数据处理）、matplotlib（可视化），不使用多余复杂库，贴合校招技术要求。

2\.  定价功能：支持欧式看涨/看跌期权定价，核心实现Black\-Scholes模型，额外补充二叉树模型、蒙特卡洛模拟两种定价方法（可选一种即可，优先简洁易理解）。

3\.  风险指标：计算Delta、Gamma、Vega、Theta、Rho全希腊字母，可直接输出风险敞口数值，贴合场外衍生品敞口监控工作场景。

4\.  辅助功能：实现隐含波动率求解，可根据标的价格、期权价格反推隐含波动率；绘制期权价格随标的价格变化的曲线、希腊字母变化曲线，用于可视化展示。

5\.  参数配置：支持手动输入期权核心参数（标的价格、行权价、到期期限、无风险利率、波动率、分红率），适配场外香草期权常见参数设置。

\#\#\# 代码要求（必满足）

1\.  分模块编写：分为定价模块（bs\_pricing\.py）、希腊字母计算模块（greeks\_calc\.py）、可视化模块（visualization\.py）、主程序（main\.py），结构清晰，便于阅读和修改。

2\.  注释完整：关键代码（如BS模型公式实现、希腊字母计算逻辑）添加详细注释，说明公式含义、参数含义，体现金融工程基础。

3\.  可运行性：编写测试代码，提供1\-2组测试用例（如中证500指数期权参数），运行后可直接输出定价结果、希腊字母数值和可视化图表。

4\.  简洁适配：避免冗余代码，不实现复杂的工业级功能，重点突出核心定价和风险计算能力，贴合校招项目展示需求。

\#\#\# 额外要求（加分项）

1\.  增加参数校验功能，避免输入无效参数（如到期期限为负数、波动率超过1等），返回友好提示。

2\.  在主程序中添加交互界面（简单命令行即可），可手动输入参数，实时输出定价结果和风险指标。

3\.  附带简单的运行说明，说明如何安装依赖库、运行程序、查看结果，方便面试官快速运行测试。

\#\#\# 输出内容

1\.  完整的Python代码（分模块文件），复制后可直接保存为\.py文件运行。

2\.  测试用例及运行结果示例，说明代码可正常运行。

3\.  简单的依赖库安装命令（如pip install py\_vollib numpy pandas matplotlib）。